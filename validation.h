#ifndef VALIDATION_H
#define VALIDATION_H
#include "atm.h"
int is_name(char []);
int is_phone(char []);
int is_email(char []);
int is_pin(char []);
int duplicate_email(Bank *, char []);
int duplicate_phone(Bank *, char []);

#endif
